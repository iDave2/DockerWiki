name: wiki
#
#  Compose file for DockerWiki.
#
#  'docker compose' can run this project but no longer builds it.
#
services:

  view:
    image: $DW_DID/mediawiki:$TAG
    depends_on:
      - data
    environment:
      - TZ=${TZ}
    hostname: view
    networks:
      - net
    ports:
      - "${DW_MW_PORTS}"

  data:
    image: $DW_DID/mariadb:$TAG
    environment:
      - TZ=${TZ}
    hostname: data
    networks:
      - net
    volumes:
      - data:/var/lib/mysql

networks:
  net:

volumes:
  data:
